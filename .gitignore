# ── 빌드 산출물 ──
build/
.gradle/
out/
*.class
run/
runs/

# ── 배포 jar ──
# 소스와 함께 두면 저장소가 매 릴리스마다 무거워진다. 배포본은 Releases 로 올린다.
dist/
*.jar
!gradle/wrapper/gradle-wrapper.jar

# ── 서버 리소스팩에서 추출한 이미지 (공개 금지) ──
# 띵타이쿤 서버의 아트 에셋이다. 우리 저작물이 아니므로 공개 저장소에 올리면
# 서버 자산을 무단 재배포하는 셈이 된다. 아이콘 매칭 작업용 로컬 참고 자료로만 둔다.
리소스팩_아이콘/
리소스팩_아이콘_최신/
카테고리_아이콘/
향장품_아이콘/
아이콘_전체보기.png
새아이콘_확인.png
아이콘후보_*.png
오로라*.png

# ── 작업용 임시 스냅샷 ──
dev/

# ── 에디터/OS ──
.idea/
*.iml
.vscode/
.DS_Store
Thumbs.db
desktop.ini
